---
import RoutePreview from '../../components/pages/RoutePreview.astro';
import { pageContent } from '../../i18n/site';
import BaseLayout from '../../layouts/BaseLayout.astro';

const page = pageContent.en.about;
---

<BaseLayout locale="en" route="about" title={`${page.title} | AndOr`} description={page.meta} noindex>
  <RoutePreview locale="en" route="about" />
</BaseLayout>
