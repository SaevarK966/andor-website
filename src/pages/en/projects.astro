---
import RoutePreview from '../../components/pages/RoutePreview.astro';
import { pageContent } from '../../i18n/site';
import BaseLayout from '../../layouts/BaseLayout.astro';

const page = pageContent.en.projects;
---

<BaseLayout locale="en" route="projects" title={`${page.title} | AndOr`} description={page.meta} noindex>
  <RoutePreview locale="en" route="projects" />
</BaseLayout>
