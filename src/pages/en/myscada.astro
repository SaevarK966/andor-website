---
import RoutePreview from '../../components/pages/RoutePreview.astro';
import { pageContent } from '../../i18n/site';
import BaseLayout from '../../layouts/BaseLayout.astro';

const page = pageContent.en.myscada;
---

<BaseLayout locale="en" route="myscada" title={`${page.title} | AndOr`} description={page.meta} noindex>
  <RoutePreview locale="en" route="myscada" />
</BaseLayout>
